@model UniStore.Models.ViewModels.Manufacturer.ManufacturerVM

@{
    ViewBag.Title = "Manufacturers";
}
<div class="width-custom">
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Manufacturer</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ContactInformation)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ContactInformation)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="">
                <input type="submit" value="Delete" class="btn btn-danger" />
                @Html.ActionLink("Back to List", "All", new { area = "" }, new { @class = "btn btn-success" })
            </div>
        }
    </div>
</div>